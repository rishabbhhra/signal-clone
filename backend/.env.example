# Secret key for JWT (change this to a random string in production)
SECRET_KEY=your-super-secret-key-change-this

# Database URL (SQLite by default, Railway persists this via volume)
DATABASE_URL=sqlite+aiosqlite:///./signal.db

# Upload directory
UPLOAD_DIR=uploads
